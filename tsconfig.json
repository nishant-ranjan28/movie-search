{
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }],
  "compilerOptions": {
    "baseUrl": ".",
    // note: paths is duplicated from tsconfig.app.json for shadcn CLI preflight; keep in sync.
    "paths": { "@/*": ["./src/*"] }
  }
}
